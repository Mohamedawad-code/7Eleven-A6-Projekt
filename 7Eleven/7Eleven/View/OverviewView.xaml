<UserControl x:Class="_7Eleven.OverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_7Eleven"
             xmlns:vm="clr-namespace:_7Eleven.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="670">

    <UserControl.Resources>
        <!-- Nav button -->
        <Style x:Key="NavBtn" TargetType="Button">
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="14,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter VerticalAlignment="Center"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E8F5EE"/>
                                <Setter Property="Foreground" Value="#006B3C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Active nav button -->
        <Style x:Key="NavBtnActive" TargetType="Button" BasedOn="{StaticResource NavBtn}">
            <Setter Property="Background" Value="#E8F5EE"/>
            <Setter Property="Foreground" Value="#006B3C"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <!-- DataGrid style -->
        <Style TargetType="DataGrid">
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F6"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EEEEEE"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#006B3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#005530"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F5F5F0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left sidebar navigation -->
        <Border Grid.Column="0" Background="White"
                BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,16,0,0">
                <TextBlock Text="NAVIGATION" FontFamily="Georgia" FontSize="9"
                           Foreground="#999999" FontWeight="Bold"
                           Margin="14,0,0,10"/>

                <Button Content="🏠  Hjem" Style="{StaticResource NavBtn}"
                        Click="Home_Click" Height="44" Width="160"/>

                <Button Content="📊  Oversigt" Style="{StaticResource NavBtnActive}"
                        Height="44" Width="160"/>

                <Button Content="➕  Registrer Vare" Style="{StaticResource NavBtn}"
                        Click="RegisterProductView_Click" Height="44" Width="160"/>

                <Button Content="🗑️  Afskriv Vare" Style="{StaticResource NavBtn}"
                        Click="DeprecationView_Click" Height="44" Width="160"/>

                <Button Content="← Tilbage" Style="{StaticResource NavBtn}"
                        Click="Back_Click" Height="44" Width="160"
                        Margin="0,20,0,0"/>
            </StackPanel>
        </Border>

        <!-- Right content -->
        <Grid Grid.Column="1" Margin="20,20,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Page title -->
            <StackPanel Grid.Row="0" Margin="0,0,0,6">
                <TextBlock Text="Oversigt Over Varer" FontFamily="Georgia" FontSize="20"
                           FontWeight="Bold" Foreground="#1A1A1A"/>
                <TextBlock Text="Alle registrerede varer" FontFamily="Georgia" FontSize="12"
                           Foreground="#888888" Margin="0,2,0,0"/>
            </StackPanel>

            <!-- Divider -->
            <Rectangle Grid.Row="1" Height="1" Fill="#E0E0E0" Margin="0,0,0,12"/>

            <!-- DataGrid bound to ProductViewModel -->
            <DataGrid Grid.Row="2"
                      x:Name="ProductDataGrid"
                      AutoGenerateColumns="False"
                      ItemsSource="{Binding Products}"
                      SelectedItem="{Binding SelectedProduct}"
                      SelectionChanged="DataGrid_SelectionChanged">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Varenavn" 
                                        Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Antal" 
                                        Binding="{Binding Amount}" Width="80"/>
                    <DataGridTextColumn Header="Modtagelsesdato" 
                                        Binding="{Binding TimeReceived, StringFormat=dd/MM/yyyy}" Width="*"/>
                    <DataGridTextColumn Header="Udløbsdato" 
                                        Binding="{Binding ExpiringDate, StringFormat=dd/MM/yyyy}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>

